<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieTek" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">#c0e863</item>
        <item name="android:windowBackground">@color/white</item>

        <item name="colorOnPrimary">#1c1f1f</item>
    </style>

    <style name="Theme.MovieTek" parent="Base.Theme.MovieTek" />

    <style name="AppTheme.Launcher" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/white</item>
        <item name="windowSplashScreenAnimationDuration">1300</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.MovieTek</item>
    </style>

    <style name="ratingBarYellow" parent="Theme.AppCompat">
        <item name="colorControlNormal">#D4AF37</item>
        <item name="colorControlActivated">#D4AF37</item>
    </style>

</resources>